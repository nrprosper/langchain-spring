spring:
  application:
    name: lang-ai
  datasource:
    url: jdbc:h2:mem:lang-ai
    driver-class-name: org.h2.Driver
    username: devkiki
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

gemini:
  api_key: